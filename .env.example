# Optional bootstrap credentials. These are copied into the client on startup
# and used only when localStorage does not already have a saved value.
# GEMINI_API_KEY=
# OPENAI_API_KEY=
# NEXT_PUBLIC_POLLINATIONS_API_KEY=  # Free key at enter.pollinations.ai (required for Pollinations fallback)
# DROPBOX_ACCESS_TOKEN=

# Node environment
NODE_ENV=development
