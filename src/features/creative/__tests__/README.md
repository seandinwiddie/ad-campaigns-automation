# Creative Tests

These tests describe how product-level creative jobs are expanded and tracked.

## Files
- `creativeSlice.test.ts`: Verifies that each product expands into three aspect-ratio variants, that completed variants update status and output URLs, and that progress rolls up correctly.
