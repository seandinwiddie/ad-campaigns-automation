# Creative Processing

This folder contains the image-processing helpers used during composition.

## Files
- `composeCreativeThunk.ts`: Uses Sharp plus SVG text overlays to resize a source image and composite campaign copy onto it.
- `resizeImageThunk.ts`: Provides file-based and buffer-based resizing helpers that normalize images into the target PNG dimensions.
