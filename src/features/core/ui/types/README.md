# UI Types

This folder contains the data contracts for the app-wide UI state.

## Files
- `appPageType.ts`: Enumerates the supported top-level screens in the SPA workflow.
- `uiStateType.ts`: Defines the full UI-state shape, including transient inputs, validation statuses, navigation, and elapsed time.
