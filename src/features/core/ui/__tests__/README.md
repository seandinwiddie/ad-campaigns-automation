# UI Tests

These tests treat the UI slice as the contract for the single-page application shell.

## Files
- `uiSlice.test.ts`: Verifies default routing, page changes, brief draft/example loading, temporary credential input tracking, and elapsed-time reducers.
