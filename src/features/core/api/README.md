# Core API

This area bridges the browser-facing app with external providers.

## Composition
- `client/`: Low-level Leonardo REST client used by the server routes.
- `slice/`: RTK Query layer used by the browser for internal route calls and Dropbox requests.
