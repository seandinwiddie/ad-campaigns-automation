# Core Types

This folder holds shared type aliases that span the core infrastructure layer.

## Files
- `coreDomainType.ts`: Constrains shared domain references to the known core areas: `api`, `settings`, and `ui`.
