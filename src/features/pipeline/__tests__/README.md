# Pipeline Tests

These tests lock down the reducer contract for the automation state machine.

## Files
- `pipelineSlice.test.ts`: Verifies major stage transitions, start timestamps, product-level success and failure updates, completion-metric calculation, and full reset behavior.
