# Brief Tests

These tests define the accepted brief formats and the clear-brief workflow contract.

## Files
- `briefSlice.test.ts`: Verifies JSON and YAML parsing, defaulting, and validation errors for incomplete briefs.
- `briefListener.test.ts`: Confirms that clearing the brief editor resets both the UI draft text and the parsed brief state together.
