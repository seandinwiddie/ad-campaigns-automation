# App API

This folder is the App Router namespace for backend endpoints exposed to the browser.

## Composition
- `leonardo/`: Provider-specific routes that proxy browser requests through server-side Leonardo logic.
